@model IEnumerable<Locadora.Models.Genero>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ds_nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dt_criacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.st_ativo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ds_nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dt_criacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.st_ativo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.cd_genero">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.cd_genero">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.cd_genero">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
